name: Reconcile review apps on AKS

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Only display stale review apps; do not delete"
        required: true
        default: true
        type: boolean
  schedule:
    - cron: "0 08 * * 0"

permissions:
  id-token: write
  pull-requests: write
  contents: read

env:
  GLOBAL_CONFIG_PATH: global_config
  TF_VARS_PATH: terraform/application/config
  TF_VARS_FILE_LOCATION: review/variables.tfvars.json
  TERRAFORM_BASE: terraform/application
  SERVICE_NAME: apply-for-qts
  RESOURCE_GROUP_NAME: s189t01-afqts-rv-rg
  STORAGE_ACCOUNT_NAME: s189t01afqtstfstatervsa
  CONTAINER_NAME: afqts-tfstate

jobs:
  display-review-apps-to-remove:
    name: Reconcile review apps
    runs-on: ubuntu-latest

    environment: review

    outputs:
      stale_prs: ${{ steps.reconcile.outputs.stale_prs }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Reconcile review apps
        id: reconcile
        uses: DFE-Digital/github-actions/review-app-reconcile@master
        with:
          azure-subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          resource-group-name: ${{ env.RESOURCE_GROUP_NAME }}
          storage-account-name: ${{ env.STORAGE_ACCOUNT_NAME }}
          terraform-base: ${{ env.TERRAFORM_BASE }}
          service-name: ${{ env.SERVICE_NAME }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          github-repo: ${{ github.repository }}
          tf-vars-path: ${{ env.TF_VARS_PATH }}
          global-config-path: ${{ env.GLOBAL_CONFIG_PATH }}
          tf-vars-filename: ${{env.TF_VARS_FILE_LOCATION  }}

  delete-review-apps-after-reconcile:
    name: Delete review app ${{ matrix.pr_number }} after reconcile
    needs: display-review-apps-to-remove
    runs-on: ubuntu-latest
    environment: review

    if: >
      (
        github.event_name == 'schedule' ||
        github.event.inputs.dry_run == 'false'
      ) &&
      needs.display-review-apps-to-remove.outputs.stale_prs != '[]'

    strategy:
      max-parallel: 1
      fail-fast: false
      matrix:
        pr_number: ${{ fromJson(needs.display-review-apps-to-remove.outputs.stale_prs) }}

    concurrency: deploy_review_${{ matrix.pr_number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete stale review app
        uses: DFE-Digital/github-actions/delete-review-app@master
        env:
          PULL_REQUEST_NUMBER: ${{ matrix.pr_number }}
        with:
          azure-subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          azure-client-id: ${{ secrets.AZURE_CLIENT_ID }}
          azure-tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          gcp-wip: projects/385922361840/locations/global/workloadIdentityPools/apply-for-qualified-teacher-sta3/providers/apply-for-qualified-teacher-sta3
          gcp-project-id: apply-for-qts-in-england
          terraform-base: ${{ env.TERRAFORM_BASE }}
          pr-number: ${{ matrix.pr_number }}
          resource-group-name: ${{ env.RESOURCE_GROUP_NAME }}
          storage-account-name: ${{ env.STORAGE_ACCOUNT_NAME }}
          container-name: ${{ env.CONTAINER_NAME }}
          tf-state-file: terraform-${{ matrix.pr_number }}.tfstate
