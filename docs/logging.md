# Logging

## Overview

The service uses rails_semantic_logger to emit structured JSON logs to stdout. These are collected and shipped to Logit, where they can be searched and filtered.

Logging is configured in config/environments/production.rb.

Relevant links to our deployed environment logs are pinned within the _REC Apply for QTS Developers_ team channel in Teams.
