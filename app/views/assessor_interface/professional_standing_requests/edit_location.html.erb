<% title = "Verify LoPS" %>

<%= render(TaskList::Component.new(AssessorInterface::VerifyLopsShowViewObject.new(assessment:, current_staff:).verify_lops_task_list_section)) %>

<% content_for :page_title, "#{"Error: " if @form.errors.any?}#{title}" %>
<% content_for :back_link_url, assessor_interface_application_form_path(@application_form) %>


