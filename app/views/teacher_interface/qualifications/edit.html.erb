<% content_for :page_title, title_with_error_prefix(t("application_form.tasks.sections.qualifications"), error: @form.errors.any?) %>
<% content_for :back_link_url, back_history_path(default: teacher_interface_application_form_path) %>

<%= render "heading", view_object: @view_object %>

<%= render "form", form: @form, qualification: @qualification %>
